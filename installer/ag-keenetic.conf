# AdGuard Home ↔ Keenetic Integration Config
# Sourced by S99adguardhome (init) and 99-adguard-dns.sh (netfilter hook)
# Change only if you understand the implications.

AG_LISTEN_IP="127.0.0.1"
AG_LISTEN_PORT="5354"
AG_POLICY_NAME="adguard-clients"
